import { InterpolationWithTheme } from "@emotion/core";

const listStyle: InterpolationWithTheme<any> = {
	listStyle: "none",
  border: "2px solid #aaa",
  height: "550px",
  overflowY: "scroll"
}

export {listStyle}